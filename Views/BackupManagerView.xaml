<Window x:Class="Cloud_Backup_Core.Views.BackupManagerView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Cloud_Backup_Core.Views"
        mc:Ignorable="d"
        Title="BackupManagerView" Height="450" Width="800">
	<Grid Margin="20">
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="*"/>
		</Grid.RowDefinitions>

		<!-- Backup Folder Selection -->
		<TextBlock Text="Backup Folder:" FontWeight="Bold"/>
		<StackPanel Orientation="Horizontal" Grid.Row="1" Margin="0,5,0,10">
			<TextBox x:Name="txtBackupFolder" Width="300" IsReadOnly="True"/>
			<Button Content="Browse..." Margin="5,0,0,0" />
		</StackPanel>

		<!-- Database Name Input -->
		<TextBlock Text="Database Name:" FontWeight="Bold" Grid.Row="2"/>
		<TextBox x:Name="txtDatabaseName" Grid.Row="3" Width="300" Margin="0,5,0,10"/>

		<!-- Backup Now Button -->
		<Button Content="Backup Now" Grid.Row="4" Height="35" Background="Green" Foreground="White"/>

		<!-- Backup Status -->
		<TextBlock x:Name="lblStatus" Text="Status: Waiting..." Grid.Row="5" Margin="0,10,0,0" FontWeight="Bold"/>

		<!-- Schedule Backup -->
		<StackPanel Grid.Row="6" Margin="0,10,0,0">
			<TextBlock Text="Schedule Backup (Daily at HH:mm):" FontWeight="Bold"/>
			<StackPanel Orientation="Horizontal">
				<TextBox x:Name="txtScheduleTime" Width="50" Text="02:00"/>
				<Button Content="Set Schedule" Margin="10,0,0,0" />
			</StackPanel>
		</StackPanel>
	</Grid>
</Window>
