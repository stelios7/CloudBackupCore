<Window x:Class="Cloud_Backup_Core.Views.BackupManagerView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Cloud_Backup_Core.Views"
        mc:Ignorable="d"
        Title="BackupManagerView" Height="400" Width="500"
		Style="{StaticResource MainWindowStyle}"
		>
	<Grid Margin="20">
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="auto"/>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="Auto"/>
		</Grid.RowDefinitions>

		<!--Backup Folder Selection-->
		<TextBlock Text="Backup Folder:" FontWeight="Bold"/>
		<StackPanel Orientation="Horizontal" Grid.Row="1" Margin="0,5,0,10">
			<TextBox Text="{Binding BackupFolder, UpdateSourceTrigger=PropertyChanged}" Width="300"/>
			<Button 
				Content="Browse..." Margin="5,0,0,0" 
				Style="{StaticResource MenuBarButton}"
				Command="{Binding BrowseCommand}" />

		</StackPanel>

		<!--SQL Server Instance Name-->
		<TextBlock Text="SQL Server Instance:" FontWeight="Bold" Grid.Row="2" />
		<TextBox x:Name="txtSqlServer" Grid.Row="3" Width="300" Margin="0,5,0,10"
				 Text="{Binding SqlServerName, UpdateSourceTrigger=PropertyChanged}"
				 HorizontalAlignment="Left"/>
		
		<!--Database Name Input-->
		<TextBlock Text="Database Name:" FontWeight="Bold" Grid.Row="4"/>
		<TextBox x:Name="txtDatabaseName" Grid.Row="5" Width="300" Margin="0,5,0,10"
				 Text="{Binding DatabaseName, UpdateSourceTrigger=PropertyChanged}"
				 HorizontalAlignment="Left"/>

		<!--Backup Now Button-->
		<StackPanel Grid.Row="6" Orientation="Horizontal" HorizontalAlignment="Center">
			<Button Content="Backup Now" Height="35" Width="200"
					Style="{StaticResource StyledButton}"
					Command="{Binding BackupCommand}" />
		</StackPanel>

		<!--Backup Status-->

		<StackPanel Orientation="Horizontal" Grid.Row="7">
			<TextBlock x:Name="lblStatus" Text="Status: " Margin="0,10,0,0" FontWeight="Bold"/>
			<TextBlock Text="{Binding StatusMessage}" Grid.Row="5" Margin="0,10,0,0" FontWeight="Bold"/>
		</StackPanel>



		<!--Schedule Backup-->
		<StackPanel Grid.Row="8" Margin="0,10,0,0">
			<TextBlock Text="Schedule Backup (Daily at HH:mm):" FontWeight="Bold"/>
			<StackPanel Orientation="Horizontal">
				<TextBox x:Name="txtScheduleTime" Width="50" 
						 HorizontalContentAlignment="Center"
						 Text="{Binding ScheduledTime, UpdateSourceTrigger=PropertyChanged}"
						 />
				<Button Content="Set Schedule" Margin="10,0,0,0" 
						Command="{Binding SetScheduleCommand}"
						Style="{StaticResource MenuBarButton}"/>
			</StackPanel>
		</StackPanel>
		
		<!-- Save Button -->
		<Button Content="Save Settings" Height="35" Width="200"
				Grid.Row="9"
					Style="{StaticResource StyledButton}"
					Command="{Binding SaveBackupSettingsCommand}" />
	</Grid>

	<!--<Grid Margin="20">
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="Auto"/>
		</Grid.RowDefinitions>

		<TextBlock Text="Database Name:" FontWeight="Bold"/>
		<TextBox Text="{Binding DatabaseName, UpdateSourceTrigger=PropertyChanged}" Grid.Row="1"/>

		<TextBlock Text="Backup Folder:" FontWeight="Bold" Grid.Row="2"/>
		<StackPanel Orientation="Horizontal" Grid.Row="3">
			<TextBox Text="{Binding BackupFolder, UpdateSourceTrigger=PropertyChanged}" Width="300"/>
			<Button Content="Browse" Command="{Binding BrowseCommand}" Margin="5,0,0,0"/>
		</StackPanel>

		<Button Content="Backup Now" Command="{Binding BackupCommand}" Grid.Row="4"
                Background="Green" Foreground="White" Height="35"/>

		<TextBlock Text="{Binding StatusMessage}" Grid.Row="5" FontWeight="Bold" Foreground="Blue"/>
	</Grid>-->

</Window>
